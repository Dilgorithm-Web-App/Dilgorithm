VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
# Optional: full API origin for production/static hosting (no Vite proxy). Must end with trailing slash if your paths expect it.
# VITE_API_BASE_URL=https://api.example.com/api/
